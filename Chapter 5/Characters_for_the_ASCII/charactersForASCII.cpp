#include<iostream>
using namespace std;

int main(){

    /*
        The solution is to go through a file and print out the characters 0-126 which are
        not usually printed (spaces, tabs, etc.)
    */

    return 0;
}